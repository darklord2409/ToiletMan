name: tipobot

# Production stack: built images only (no source bind-mounts, no dev
# servers), restart policies, and health checks. Postgres/Redis are not
# published to the host — only backend/admin/miniapp need to be reachable,
# and until a domain + reverse proxy (see DEPLOYMENT.md) are set up, those
# three are published directly on the ports below for direct testing.
#
# Run with:
#   docker compose -f docker-compose.prod.yml --env-file .env up -d --build

services:
  postgres:
    image: postgres:17-alpine
    restart: always
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./database/init:/docker-entrypoint-initdb.d
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      - tipobot_network

  redis:
    image: redis:7-alpine
    restart: always
    command: ["redis-server", "--appendonly", "yes", "--requirepass", "${REDIS_PASSWORD}"]
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD}", "--no-auth-warning", "ping"]
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      - tipobot_network

  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile.prod
    image: tipobot-backend-prod
    restart: always
    env_file:
      - .env
    environment:
      POSTGRES_HOST: postgres
      REDIS_HOST: redis
      ENVIRONMENT: production
      DEBUG: "false"
    ports:
      - "${BACKEND_PORT:-8000}:8000"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    volumes:
      - backend_uploads:/app/uploads
    networks:
      - tipobot_network

  bot:
    build:
      context: .
      dockerfile: bot/Dockerfile.prod
    image: tipobot-bot-prod
    restart: always
    env_file:
      - .env
    environment:
      REDIS_HOST: redis
      BACKEND_HOST: backend
      BACKEND_PORT: 8000
      BACKEND_BASE_URL: http://backend:8000/api/v1
      ENVIRONMENT: production
    depends_on:
      redis:
        condition: service_healthy
      backend:
        condition: service_started
    networks:
      - tipobot_network

  admin:
    build:
      context: ./admin
      dockerfile: Dockerfile.prod
    image: tipobot-admin-prod
    restart: always
    ports:
      - "${ADMIN_PORT:-5173}:80"
    depends_on:
      - backend
    networks:
      - tipobot_network

  miniapp:
    build:
      context: ./miniapp
      dockerfile: Dockerfile.prod
    image: tipobot-miniapp-prod
    restart: always
    ports:
      - "${MINIAPP_PORT:-5174}:80"
    depends_on:
      - backend
    networks:
      - tipobot_network

  website:
    build:
      context: ./website
      dockerfile: Dockerfile.prod
    image: tipobot-website-prod
    restart: always
    environment:
      BACKEND_INTERNAL_URL: http://backend:8000
      NEXT_PUBLIC_API_BASE_URL: /api/v1
      NEXT_PUBLIC_MEDIA_BASE_URL: /media
      NEXT_PUBLIC_SITE_URL: https://toiletman.uz
      NODE_ENV: production
    ports:
      - "${WEBSITE_PORT:-3000}:3000"
    depends_on:
      - backend
    networks:
      - tipobot_network

  # Reverse proxy + automatic Let's Encrypt HTTPS for the real domain. The
  # admin/miniapp ports above stay published too (harmless overlap) as a
  # fallback until the domain is confirmed working end-to-end -- safe to
  # remove them once DNS + certs are verified, per DEPLOYMENT.md.
  caddy:
    image: caddy:2-alpine
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
      - caddy_data:/data
    depends_on:
      - admin
      - miniapp
      - website
    networks:
      - tipobot_network

networks:
  tipobot_network:
    driver: bridge

volumes:
  postgres_data:
  redis_data:
  backend_uploads:
  caddy_data:
