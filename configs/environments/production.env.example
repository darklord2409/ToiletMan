# Overrides for production. Copy relevant values into the root .env file
# and replace every placeholder secret before deploying.
ENVIRONMENT=production
DEBUG=false
LOG_LEVEL=INFO
SECRET_KEY=replace-with-a-long-random-value
